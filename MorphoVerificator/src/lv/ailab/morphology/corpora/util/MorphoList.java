package lv.ailab.morphology.corpora.util;


import java.util.ArrayList;

import lv.semti.morphology.attributes.AttributeValues;


/**
 * Internal data structure - list of morphologicaly annotated tokens.
 */
public class MorphoList extends ArrayList<MorphoEntry>
{
/*	public boolean add(String token, AttributeValues attributes, String lemma)
	{
		return add(new MorphoEntry(token, attributes, lemma));
	}
	
	public boolean add(String token, String tag, String lemma)
	{
		return add(new MorphoEntry(token, tag, lemma));
	}
*/
}